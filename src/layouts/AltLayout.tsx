export const AltLayout: React.FC<{ children: React.ReactNode }> = ({ children }) => {
    return (
        <main>
            <h1>Alternate Layout</h1>
            {children}
        </main>
    );
};
